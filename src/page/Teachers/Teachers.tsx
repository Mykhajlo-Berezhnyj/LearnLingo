import TeachersList from "../../components/TeachersList/TeachersList";
import css from "./Teachers.module.css";

export default function Teachers() {
  return <TeachersList className={css.techers} />;
}
